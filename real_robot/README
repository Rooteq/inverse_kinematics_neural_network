To create virtual environment and install packages run:
1. $ python3 -m venv env
2. $ source env/bin/activate
3. $ pip3 install -r requirements.txt

To create the dataset and train the model (best params for start shape):
$ python3 real_robot.py

To test the trained model on a star shape;
$ python3 real_robot_test.py